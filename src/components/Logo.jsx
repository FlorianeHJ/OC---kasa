import React from 'react';
import logo from '../assets/logo.png';

const Logo = () => {
    return (
        <div > 
        <img src={logo} alt="Logo Kasa" className="logo"/>
            
        </div>
    );
};

export default Logo;